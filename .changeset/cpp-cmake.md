---
"@tanstack/highlight": minor
---

Add isolated C++ and CMake language definitions with contextual strings and comments, C++ aliases and numeric literals, and CMake variables and generator expressions. Core and unrelated selective bundles are unchanged.
